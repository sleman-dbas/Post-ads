<%-include('../partial/header.ejs')-%>
<%-include('../partial/navbar.ejs')-%>
    <div class="continer">
        <div class="row mt-4">
            <div class="col-md-6 offset-md-3">
                <div class="card">
                    <div class="card-body">
                      <h5 class="card-title"><%=  event.title %></h5>
                      <hr>
                      <p class="card-text"><%=  event.descraption %></p>
                      <p><%=  event.location %></p>
                      <!-- <p><span class="badge badge-secondary" color="black"><%=  event.date %></span></p> -->
                      <p><span class="ssd" color="red"><%=  event.date.toDateString() %></span></p> 
                      <%if((user) && (user.id == event.user_id)){%>
                      <a href="<%='/events/edit/'+ event.id %>" class="btn btn-primary">Edit</a>
                      <button href="" class="btn btn-danger" onclick="deleteEvent()" id="deleteBTN" data-id="<%= event.id%>">Delete</button>
                    <%}%>
                    </div>
                  </div>
            </div>
        </div>
    </div>

    <%-include('../partial/footer.ejs')-%>
